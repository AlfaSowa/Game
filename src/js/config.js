export const config = {
    canvasWidth: innerWidth,
    canvasHeight: innerHeight,
    default: {
        color: "green",
        radius: 30,
        coordX: 200,
        coordY: 100,
    },
    TWO_PI: 2 * Math.PI,
};
